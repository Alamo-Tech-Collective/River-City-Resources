<div class="form-group">
    <label for="${property}" class="control-label">${label}</label>
    <g:textArea name="${property}" 
                value="${value}" 
                class="form-control" 
                rows="4"
                placeholder="Provide a clear description of what types of resources belong in this category"/>
    <small class="form-text text-muted">
        Help users understand what kinds of resources they'll find in this category.
    </small>
</div>